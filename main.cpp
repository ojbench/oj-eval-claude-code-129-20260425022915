#include <iostream>
#include <string>

int main() {
    // Read input from stdin
    std::string line;
    while (std::getline(std::cin, line)) {
        // Echo input to stdout (basic placeholder)
        std::cout << line << std::endl;
    }

    return 0;
}
